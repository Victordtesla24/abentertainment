{
  "name": "abentertainment-workspace",
  "private": true,
  "scripts": {
    "dev": "npm --prefix repo run dev --",
    "build": "npm --prefix repo run build --",
    "start": "npm --prefix repo run start --",
    "lint": "npm --prefix repo run lint --",
    "install:repo": "npm --prefix repo install"
  }
}
